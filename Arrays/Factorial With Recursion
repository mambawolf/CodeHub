#include <iostream>

using namespace std;

int fact(int n)
{
    {
        if(n==0)
            return 1;
    }
    int smallans=fact(n-1);
    return n*smallans;
}
int main()
{
    int n= 5;
    cout<<"The factorial of the number is "<<fact(n)<<endl;

    return 0;
}

